include:
  - project: 'alintaenergy/devsecops/checkmarx'
    file: 'templates/cx-flow.yml'

variables:
  AWS_REGION: ap-southeast-2

default:
  image:
    name: 307973494033.dkr.ecr.ap-southeast-2.amazonaws.com/dev/serverless/maven-build-centos:v25033
    pull_policy: [if-not-present, always]
  before_script:
    - |
      # Job Details
      echo "CI_COMMIT_REF_NAME = $CI_COMMIT_REF_NAME , CI_COMMIT_TAG = $CI_COMMIT_TAG , CI_COMMIT_TITLE = $CI_COMMIT_TITLE"

stages:
  - "build"
  - "deploy:test"
  - "deploy:prod"
  - "execute:test"

build:
  stage: "build"
  tags:
    - fisker
  script:
    - |
      # Install requirements
      pip3 install -r requirements.txt -r runtime-requirements.txt
    - |
      # Lint the cloudformation templates
      chmod u+x cf-lint
      ./cf-lint
    - |
      # Check python test coverage
      coverage run --branch -m pytest --durations=0 tests
      coverage report -m
  variables:
    CF_ROLE: CloudformationReleaseRole
    CI_ENVIRONMENT_NAME: dev-fisker

anstead:deploy:
  stage: "deploy:test"
  trigger:
    include: .gitlab-deploy.yml
    strategy: depend
  variables:
    CI_ENVIRONMENT_NAME: tst-anstead
    CI_PARENT_PIPELINE_ID: $CI_PIPELINE_ID
    KMS_KEY_ALIAS: corecmk
    RUNNER_TAG: anstead
  when: manual
  needs:
    - 'build'

matten:deploy:
  stage: "deploy:prod"
  trigger:
    include: .gitlab-deploy.yml
    strategy: depend
  variables:
    CI_ENVIRONMENT_NAME: prd-matten
    CI_PARENT_PIPELINE_ID: $CI_PIPELINE_ID
    KMS_KEY_ALIAS: corecmk
    RUNNER_TAG: matten
  when: manual
  only:
    - main
  needs:
    - 'build'

anstead:restore:
  stage: "execute:test"
  timeout: 24 hours
  when: manual
  tags:
    - anstead
  script:
    - python3 bin/execute-bua-steps.py
  variables:
    state_machine_arn: arn:aws:states:ap-southeast-2:561082505378:stateMachine:tst-anstead-bua
    name: Restore
    steps: Restore

anstead:clean:
  stage: "execute:test"
  timeout: 24 hours
  when: manual
  tags:
    - anstead
  script:
    - python3 bin/execute-bua-steps.py
  variables:
    state_machine_arn: arn:aws:states:ap-southeast-2:561082505378:stateMachine:tst-anstead-bua
    name: Clean
    steps: EmptyQueues,CleanWorkflows,CleanInvoiceAttribute,CleanUtilityProfile

anstead:warm:
  stage: "execute:test"
  timeout: 24 hours
  when: manual
  tags:
    - anstead
  script:
    - python3 bin/execute-bua-steps.py
  variables:
    state_machine_arn: arn:aws:states:ap-southeast-2:561082505378:stateMachine:tst-anstead-bua
    name: Warm
    steps: ScaleUpWorkflow,WarmStatistics,WarmIndexes,ScaleDown

anstead:profiles:
  stage: "execute:test"
  timeout: 24 hours
  when: manual
  tags:
    - anstead
  script:
    - python3 bin/execute-bua-steps.py
  variables:
    state_machine_arn: arn:aws:states:ap-southeast-2:561082505378:stateMachine:tst-anstead-bua
    name: Profiles
    steps: UtilityProfiles,Segments,Snapshot

anstead:mscalar:
  stage: "execute:test"
  timeout: 24 hours
  when: manual
  tags:
    - anstead
  script:
    - python3 bin/execute-bua-steps.py
  variables:
    state_machine_arn: arn:aws:states:ap-southeast-2:561082505378:stateMachine:tst-anstead-bua
    name: MScalar
    steps: Microscalar,Snapshot

anstead:basic:
  stage: "execute:test"
  timeout: 24 hours
  when: manual
  tags:
    - anstead
  script:
    - python3 bin/execute-bua-steps.py
  variables:
    state_machine_arn: arn:aws:states:ap-southeast-2:561082505378:stateMachine:tst-anstead-bua
    name: Basic
    steps: BasicReads,Snapshot

anstead:nem12:
  stage: "execute:test"
  timeout: 24 hours
  when: manual
  tags:
    - anstead
  script:
    - python3 bin/execute-bua-steps.py
  variables:
    state_machine_arn: arn:aws:states:ap-southeast-2:561082505378:stateMachine:tst-anstead-bua
    name: NEM12
    steps: ScaleUpMeterdata,GenerateNEM12,Snapshot

anstead:invoices:
  stage: "execute:test"
  timeout: 24 hours
  when: manual
  tags:
    - anstead
  script:
    - python3 bin/execute-bua-steps.py
  variables:
    state_machine_arn: arn:aws:states:ap-southeast-2:561082505378:stateMachine:tst-anstead-bua
    name: Invoices
    steps: RestartMeterdata,InvoiceRuns,Snapshot,ILIExceptions,Snapshot

anstead:down:
  stage: "execute:test"
  timeout: 24 hours
  when: manual
  tags:
    - anstead
  script:
    - python3 bin/execute-bua-steps.py
  variables:
    state_machine_arn: arn:aws:states:ap-southeast-2:561082505378:stateMachine:tst-anstead-bua
    name: Down
    steps: ScaleDown

anstead:prepare:
  stage: "execute:test"
  timeout: 24 hours
  when: manual
  tags:
    - anstead
  script:
    - python3 bin/execute-bua-steps.py
  variables:
    state_machine_arn: arn:aws:states:ap-southeast-2:561082505378:stateMachine:tst-anstead-bua
    name: Prepare
    steps: Prepare,Snapshot

anstead:export:
  stage: "execute:test"
  timeout: 24 hours
  when: manual
  tags:
    - anstead
  script:
    - python3 bin/execute-bua-steps.py
  variables:
    state_machine_arn: arn:aws:states:ap-southeast-2:561082505378:stateMachine:tst-anstead-bua
    name: Export
    steps: Export

anstead:final:
  stage: "execute:test"
  timeout: 24 hours
  when: manual
  tags:
    - anstead
  script:
    - python3 bin/execute-bua-steps.py
  variables:
    state_machine_arn: arn:aws:states:ap-southeast-2:561082505378:stateMachine:tst-anstead-bua
    name: Final
    steps: ProfileValidation,DumpErrors,SwitchBastion
